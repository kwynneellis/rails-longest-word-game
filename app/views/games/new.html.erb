<h1>New game</h1>
<p><%= @letters.join(' ') %></p>
<form action="/score" method="post" data-turbo="false">
  <!-- ... -->
  <label for="longestWordInput" class="form-label">What is the longest word you can find?</label>
    <div class="input-group mb-3">
      <input type="text" name="longestWord" class="form-control border border-1">
      <span><input type="submit" value="Play" class="btn border border-1"></span>
    </div>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= hidden_field_tag :letters, @letters %>
</form>
